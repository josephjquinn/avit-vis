
import React from "react";

const Visualize: React.FC = () => {
  return (
    <div>
      <h1 style={{color:"white"}}>This is the Visualize component</h1>
    </div>
  );
};

export default Visualize;
